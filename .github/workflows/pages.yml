name: Deploy to GitHub Pages

on:
  push:
    branches: [ gh-pages ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        run: |
          git clone https://github.com/thamiresfm/criminal-minds-game.git
          cd criminal-minds-game
          git checkout gh-pages
      
      - name: Setup Pages
        run: |
          echo "Configurando GitHub Pages..."
          echo "Branch: gh-pages"
          echo "Source: / (root)"
      
      - name: Deploy
        run: |
          echo "Deploy manual para GitHub Pages"
          echo "Acesse: https://thamiresfm.github.io/criminal-minds-game" 